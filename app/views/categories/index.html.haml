%h1 Categories
%a.btn.btn-primary{ href: new_category_path }
  %i.bi.bi-plus-circle-fill
  New Category
%a.btn.btn-primary{ href: merge_categories_path }
  %i.bi.bi-plus-circle-fill
  Merge Categories
%table.styled-table{ data: { controller: "clickable-row" } }
  %thead
    %tr
      %th{scope: "col"} Name
      %th{scope: "col"} Income
      %th{scope: "col"} Untracked
  %tbody
    - @categories.each do |category|
      %tr.clickable-row{ 'data-href': category_url(category) }
        %td= category.name
        %td= category.income
        %td= category.untracked
